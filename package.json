{
    "name": "@ramseyinhouse/steppp-monorepo",
    "private": true,
    "scripts": {
        "install-all": "lerna exec yarn install",
        "build": "lerna run build",
        "test": "lerna run test",
        "graph": "nx graph",
        "format": "prettier --write \"**/*.{js,ts,tsx,jsx,css,md,html}\"",
        "publish": "lerna publish"
    },
    "lint-staged": {
        "*.{js,ts,css,md}": "prettier --write"
    },
    "devDependencies": {
        "lerna": "^6.4.1",
        "prettier": "2.8.3"
    }
}
